name: Build and Release

on:
  push:
    branches:
      - main  # Adjust this to match your branch name

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        os: [windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'  # Adjust to your Python version

      - name: Install dependencies
        run: |
          pip install -r requirements.txt  # Adjust as needed

      - name: Build with PyInstaller
        run: |
          run: |
          pyinstaller --onefile main.py
          mkdir dist
          mv ./main.exe ./dist/main.exe
      
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.ScreenText_Token }}
        with:
          tag_name: v1.0.3
          release_name: Release v1.0.3
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.ScreenText_Token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/main.exe  # Adjust to the path of your PyInstaller output
          asset_name: main.exe  # Adjust to your binary file name
          asset_content_type: application/octet-stream
